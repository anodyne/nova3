# Changes to BlatUI components

## Combobox
- Removed the call to `<x-lucide-x />` in favor of `<x-tabler-x />`

## Number input
- Removed the call to `<x-lucide-minus />` in favor of `<x-tabler-minus />`
- Removed the call to `<x-lucide-plus />` in favor of `<x-tabler-plus />`
