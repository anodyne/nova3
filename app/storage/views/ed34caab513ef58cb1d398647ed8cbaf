<div class="navbar navbar-default navbar-fixed-top">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>

			<a href="<?php echo URL::to('/'); ?>" class="navbar-brand"><?php echo $name; ?></a>
		</div>

		<div class="navbar-collapse collapse navbar-responsive-collapse">
			<ul class="nav navbar-nav">
			<?php foreach ($items[$section]['mainNavItems'] as $mainNavItem): ?>

				<?php if (isset($items[$section][$mainNavItem->category])): ?>
					<li class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown"><?php echo $mainNavItem->name; ?> <b class="caret"></b></a>

						<ul class="dropdown-menu">
						<?php foreach ($items[$section][$mainNavItem->category] as $i): ?>
							<?php if ($i->order == 0 and $i->group != 0): ?>
								<li class="divider"></li>
							<?php endif; ?>

							<li><?php echo HTML::link($i->url, $i->name); ?></li>
						<?php endforeach; ?>
						</ul>
					</li>
				<?php else: ?>
					<li><?php echo HTML::link($mainNavItem->url, $mainNavItem->name); ?></li>
				<?php endif; ?>

			<?php endforeach; ?>
			</ul>

			<?php echo $userMenu; ?>
		</div>
	</div>
</div>