<x-filament.modal-content :$action title="Duplicate rank name?">
    <x-text variant="strong">
        Are you sure you want to duplicate the
        <strong class="font-semibold">{{ $record->name }}</strong>
        rank name?
    </x-text>
</x-filament.modal-content>
